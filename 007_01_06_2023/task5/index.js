/**
 * write function foo using "function" keyword which should duplicate an array 
 * write function bar which is arrow function "=>" it should return uniq elements
 */

const arr = [1, 2, 3, 'i', 'o', 'b', 1, 'i'];